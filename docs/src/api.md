Count-Min Sketch API
====================

```
		{meta}
		CurrentModule = CountMin
```

    {docs}
    CountMin.CountMinSketch
    CountMin.getindex()
    CountMin.add!()
    CountMin.push!
    CountMin.pop!
    CountMin.size()
    CountMin.eltype()
